#pragma once

// Incluir SDKDDKVer.h define a plataforma Windows mais alta dispon?el.

// Se voc?deseja compilar sua aplicação para vers?s anteriores de plataformas Windows, inclua WinSDKVer.h e
// ajuste a macro _WIN32_WINNT para a plataforma que voc?deseja suportar antes de incluir SDKDDKVer.h.

#include <SDKDDKVer.h>
